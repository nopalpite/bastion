Flask==3.0.3
Flask-SocketIO==5.3.6
Flask-Login==0.6.3
paramiko==3.4.0
PyYAML==6.0.1
eventlet==0.36.1
# Versions non épinglées strictement (contrairement aux autres): ces
# trois paquets ont une extension native (Rust ou C) et certaines
# versions exactes n'ont pas de wheel précompilée pour toutes les
# architectures (ex: aarch64/Raspberry Pi). Ces plages laissent pip
# choisir une version avec une wheel disponible plutôt que de forcer une
# version précise qui n'en a peut-être pas.
cryptography>=42.0.5,<44.0.0
bcrypt>=4.1.2,<5.0.0
PyNaCl>=1.5.0,<2.0.0
